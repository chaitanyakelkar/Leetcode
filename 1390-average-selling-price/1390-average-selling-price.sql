# Write your MySQL query statement below
select a.product_id, ifnull(round((sum(units * price) / sum(units)), 2), 0) as average_price from Prices as a left join UnitsSold as b on a.product_id = b.product_id where purchase_date between start_date and end_date or purchase_date is null group by a.product_id;